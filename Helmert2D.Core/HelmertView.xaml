<Window x:Class="Helmert2D.Core.HelmertView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Helmert2D.Core"
        mc:Ignorable="d"
        Title="Helmert 2D" Height="500" Width="700"
        Background="#1E1E1E" Foreground="White"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Margin" Value="2"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0097FB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="RowBackground" Value="#252526"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="True"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#333337"/>
            <Setter Property="Foreground" Value="#F1F1F1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="#007ACC"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Helmert 2D Transformation" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,15"/>

        <GroupBox Grid.Row="1" Header="Control Points (Source -> Target)">
            <DataGrid x:Name="PointsGrid" Margin="5">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Add Point" Click="AddPoint_Click"/>
                        <MenuItem Header="Delete Selected Point(s)" Click="DeletePoint_Click"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Use" Binding="{Binding IsActive}" Width="40"/>
                    <DataGridTextColumn Header="Source X" Binding="{Binding SourceX, StringFormat=F3}" Width="*"/>
                    <DataGridTextColumn Header="Source Y" Binding="{Binding SourceY, StringFormat=F3}" Width="*"/>
                    <DataGridTextColumn Header="Target X" Binding="{Binding TargetX, StringFormat=F3}" Width="*"/>
                    <DataGridTextColumn Header="Target Y" Binding="{Binding TargetY, StringFormat=F3}" Width="*"/>
                    <DataGridTextColumn Header="Resid X" Binding="{Binding ResidualX, StringFormat=F3}" Width="80" IsReadOnly="True" Foreground="#FF8888"/>
                    <DataGridTextColumn Header="Resid Y" Binding="{Binding ResidualY, StringFormat=F3}" Width="80" IsReadOnly="True" Foreground="#FF8888"/>
                    <DataGridTextColumn Header="Dist Res" Binding="{Binding ResidualDist, StringFormat=F3}" Width="80" IsReadOnly="True" Foreground="#FF8888"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Results">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Translation X:" Grid.Row="0" Grid.Column="0"/>
                <TextBox x:Name="TxtTransX" Grid.Row="0" Grid.Column="1" IsReadOnly="True"/>

                <TextBlock Text="Translation Y:" Grid.Row="0" Grid.Column="3"/>
                <TextBox x:Name="TxtTransY" Grid.Row="0" Grid.Column="4" IsReadOnly="True"/>

                <TextBlock Text="Rotation (deg):" Grid.Row="1" Grid.Column="0"/>
                <TextBox x:Name="TxtRotation" Grid.Row="1" Grid.Column="1" IsReadOnly="True"/>

                <TextBlock Text="Scale Factor:" Grid.Row="1" Grid.Column="3"/>
                <TextBox x:Name="TxtScale" Grid.Row="1" Grid.Column="4" IsReadOnly="True"/>

                <TextBlock Text="RMSE:" Grid.Row="2" Grid.Column="0" Foreground="#FF5555"/>
                <TextBox x:Name="TxtRMSE" Grid.Row="2" Grid.Column="1" IsReadOnly="True"/>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="3" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
                <CheckBox x:Name="ChkComputeScale" Content="Calculate Scale (Best Fit)" IsChecked="True" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <CheckBox x:Name="ChkTransformCopy" Content="Transform Copy (Keep Original)" IsChecked="False" Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="BtnPick" Content="PICK POINTS" Grid.Column="0" Margin="0,0,5,0" Click="BtnPick_Click" Background="#CA5100"/>
                <Button x:Name="BtnCalculate" Content="CALCULATE" Grid.Column="1" Margin="5,0,5,0" Click="BtnCalculate_Click"/>
                <Button x:Name="BtnApply" Content="APPLY TO DRAWING" Grid.Column="2" Margin="5,0,5,0" Click="BtnApply_Click" IsEnabled="False" Background="#2E7D32"/>
                <Button x:Name="BtnClose" Content="CLOSE" Grid.Column="3" Margin="5,0,0,0" Background="#3E3E42" Click="BtnClose_Click"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>